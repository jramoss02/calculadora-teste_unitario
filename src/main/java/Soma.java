/**
 * @author José Ramos
 * @version 1.03
 * @since Release 1.02
 *
 * Classe responsável por realizar operações de soma.
 */
public class Soma {
    /**
     * Realiza a operação de soma de dois números inteiros.
     * @param a O primeiro número a ser somado.
     * @param b O segundo número a ser somado.
     * @return O resultado da soma.
     */
    public int somar(int a, int b) {
        // Realiza a soma de 'a' e 'b' e retorna o resultado
        return a + b;
    }
}
